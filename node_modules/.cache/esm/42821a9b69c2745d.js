let res,Cloudinary,ImageKit;_69c‍.x([["default",()=>_69c‍.o]]);_69c‍.w("express/lib/response",[["default",["res"],function(v){res=v}]]);_69c‍.w("../services/Cloudinary",[["default",["Cloudinary"],function(v){Cloudinary=v}]]);_69c‍.w("../services/ImageKit",[["default",["ImageKit"],function(v){ImageKit=v}]]);






class FileManager {

    constructor(service, req) {
        this.service = service;
        this.cloudinary = new Cloudinary(req.file);
        this.imageKit = new ImageKit(req.file);
    }

    //For Images 
    upload() {
        if(this.service === "cloudinary") {
            return this.cloudinary.upload();
        }
        if(this.service === "imagekit") {
            return this.imageKit.upload();
        }
    }

    find() {
        if(this.service === "cloudinary") {
            return this.cloudinary.find();
        }
        if(this.service === "imagekit") {
            return this.imageKit.find();
        }
    }

    delete() {
        if(this.service === "cloudinary") {
            return this.cloudinary.delete();
        }
        if(this.service === "imagekit") {
            return this.imageKit.delete();
        }
    }

}


_69c‍.d(FileManager);