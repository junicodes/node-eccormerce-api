let authenticate;_e34‍.x([["isOnlyAdmin",()=>isOnlyAdmin]]);_e34‍.w("./authenticate",[["authenticate",["authenticate"],function(v){authenticate=v}]]);

       const isOnlyAdmin = (req, res, next) => {
    
    if(!req.user.isAdmin) {
        return res.status(403).json({
            message: "You are not permitted to access this route!"
        });
    }

    next();
}